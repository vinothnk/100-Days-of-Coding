#waiter -> is an object in a class

#1. what waiter has -> attributes -> mainly variables

is_holding_place = True
tables_responsible = [4,5,6]

#2. what waiter does -> methods -> mainly functions
def take_order(table, order):
    #takes order to chef

def take_payment(amount):
    #takes money for the restaurant


class Waiter(): #main class -> holds all waiters(objects) in the restaurant


car = CarBlueprint() #car -> object, CarBlueprint() -> class

#car has attributes -> speed, fuel
car.speed() = 0
car.fuel() = 32
#calling speed method from car object

#car has methods -> function to move, function to stop
def move():
    speed = 60

def stop():
    speed = 0